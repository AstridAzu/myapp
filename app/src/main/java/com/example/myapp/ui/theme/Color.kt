package com.example.myapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colores extraídos de LoginScreen.kt, MainScreen.kt y Material defaults
val md_theme_light_primary = Color(0xFF007BFF) // Botón Iniciar Sesión (Login)
val md_theme_light_onPrimary = Color(0xFFFFFFFF)
val md_theme_light_secondary = Color(0xFF6C757D) // Botón Registrarse (Login)
val md_theme_light_onSecondary = Color(0xFFFFFFFF)
val md_theme_light_background = Color(0xFFF8F9FA) // Fondo general (Login/Main)
val md_theme_light_onBackground = Color(0xFF212529) // Texto principal (Login/Main)
val md_theme_light_surface = Color(0xFFFFFFFF) // Fondo campos texto (Login), Card Categorías (Main)
val md_theme_light_onSurface = Color(0xFF212529)

// Colores específicos de MainScreen.kt
val DarkAppBarColor = Color(0xFF050D17) // Top bar oscura
val CategoryCardColor = Color(0xFF31CAF8) // Color de las tarjetas de categoría
val CategoryCardIconColor = Color(0xFFFFFFFF) // Color del icono dentro de las tarjetas

// Colores personalizados existentes
val BackgroundLight = Color(0xFFDBE6FA)
val TextPrimaryDark = Color(0xFFFFFFFF)
val TextPrimaryLight = Color(0xFF212529)
val TextSecondaryLight = Color(0xFF6C757D)

// Colores para el tema
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
